import { NavLink } from "react-router-dom";

const CreateMatch = (props) => {
  return (
    <div>
      <p>SEI NEL CREATE MATCH</p>
      <NavLink to="/matchdetails">Crea Squadre</NavLink>
    </div>
  );
};

export default CreateMatch;
